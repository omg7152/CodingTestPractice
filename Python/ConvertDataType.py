# # 형변환
# tempset = {"커피", "우유", "주스"};
# print(tempset, type(tempset));

# tempset = list(tempset); # 리스트로 변환
# print(tempset, type(tempset));

# tempset = tuple(tempset); # 튜플로 변환
# print(tempset, type(tempset));

# tempset = set(tempset); # 집합으로 변환
# print(tempset, type(tempset));
